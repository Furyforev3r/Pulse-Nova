local config = {}

config.keys = {"a", "s", "j", "k"}
config.columns = {200, 250, 300, 350}
config.noteSpeed = 300

config.hitWindows = {
    sick = 0.03,
    good = 0.08,
    ok = 0.15
}

config.hitSize = 100
config.hitZoneY = 350

return config